import { Link } from 'react-router-dom';

const Navbar = () => {
  return (
    <nav className="a">
      <Link to="/"></Link>
      <Link to="/create"></Link>
    </nav>
  );
};

export default Navbar;
